import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { action } from '@storybook/addon-actions'

import AppWrapper from '@/test/supports/AppWrapper'
import CircleTime from '.'
import Space from 'stories/components/styled-system/Space.mdx'
import Styles from 'stories/styles.mdx'

<Meta title="Story/Components/CircleTime" component={CircleTime} />

<Styles />

# CircleTime

With `MDX` we can define a story for `CircleTime` right in the middle of our
Markdown documentation.

export const Template = (args) => <AppWrapper><CircleTime {...args} /></AppWrapper>

<Canvas>
  <Story
    name="Normal"
    args={{ variant: 'normal' }}
    onClick={action('Medium CircleTime clicked')}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<div class="subheading">Props</div>

<Space />
