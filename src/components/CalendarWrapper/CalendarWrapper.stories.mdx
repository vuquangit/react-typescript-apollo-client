import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { action } from '@storybook/addon-actions'
import CalendarWrapper from '.'
import Styles from 'stories/styles.mdx'
import AppWrapper from '@/test/supports/AppWrapper'
import AttendanceDetail from '@/components/AttendanceDetail'

<Meta title="Story/Components/CalendarWrapper" component={CalendarWrapper} />

<Styles />

# CalendarWrapper

With `MDX` we can define a story for `CalendarWrapper` right in the middle of our
Markdown documentation.

export const TemplateLight = (args) => (
  <AppWrapper themeMode="light">
    <CalendarWrapper {...args}>
      <AttendanceDetail />
    </CalendarWrapper>
  </AppWrapper>
)

export const TemplateDark = (args) => (
  <AppWrapper themeMode="dark">
    <CalendarWrapper {...args}>
      <AttendanceDetail />
    </CalendarWrapper>
  </AppWrapper>
)

<Canvas>
  <Story name="Light">{TemplateLight.bind({})}</Story>
  <Story name="Dark">{TemplateDark.bind({})}</Story>
</Canvas>
