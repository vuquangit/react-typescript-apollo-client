import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { action } from '@storybook/addon-actions'
import Emoji from '.'
import Space from 'stories/components/styled-system/Space.mdx'
import Styles from 'stories/styles.mdx'

<Meta title="Story/Components/Emoji" component={Emoji} />

<Styles />

# Emoji

With `MDX` we can define a story for `Emoji` right in the middle of our
Markdown documentation.

export const Template = (args) => <Emoji {...args} />

<Canvas>
  <Story
    name="Normal"
    args={{ variant: 'normal' }}
    onClick={action('Medium Emoji clicked')}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<div class="subheading">Props</div>

<Space />
