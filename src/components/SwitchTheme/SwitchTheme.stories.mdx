import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { action } from '@storybook/addon-actions'
import SwitchTheme from '.'
import AppWrapper from '@/test/supports/AppWrapper'
import Space from 'stories/components/styled-system/Space.mdx'
import Styles from 'stories/styles.mdx'

<Meta
  title="Story/Components/SwitchTheme"
  component={SwitchTheme}
  argTypes={{ onClick: { action: 'clicked' } }}
/>

<Styles />

# SwitchTheme

With `MDX` we can define a story for `SwitchTheme` right in the middle of our
Markdown documentation.

export const Template = (args) => (
  <AppWrapper themeMode="light">
    <SwitchTheme {...args} />
  </AppWrapper>
)

<Canvas>
  <Story name="Normal">{Template.bind({})}</Story>
</Canvas>

<div class="subheading">Props</div>

<Space />
