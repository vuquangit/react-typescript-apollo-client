import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { action } from '@storybook/addon-actions'
import Button from '.'
import Space from 'stories/components/styled-system/Space.mdx'
import Styles from 'stories/styles.mdx'

<Meta title="Story/Component/Button" component={Button} />

<Styles />

# Button

With `MDX` we can define a story for `Button` right in the middle of our
Markdown documentation.

export const Template = (args) => <Button {...args} />

<Canvas>
  <Story
    name="Large"
    args={{ size: 'large' }}
    onClick={action('Large button clicked')}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Medium"
    args={{ size: 'medium' }}
    onClick={action('Medium button clicked')}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Disable"
    args={{ disabled: true }}
    onClick={action('Disable button clicked')}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<div class="subheading">Props</div>

<Space />
